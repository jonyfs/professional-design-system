---
"@jonyfs/react": major
---

Renamed the published package from `@professional-design-system/react` to `@jonyfs/react`. The `@professional-design-system` npm org either doesn't exist or isn't publishable to; publishing under the personal `jonyfs` account instead. Consumers must update their import specifier from `@professional-design-system/react` to `@jonyfs/react`.
