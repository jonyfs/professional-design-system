---
"professional-design-system": major
---

Renamed the published package to `professional-design-system` (previously `@jonyfs/react`, and before that `@professional-design-system/react`). Consumers must update their import specifier to `professional-design-system`.
