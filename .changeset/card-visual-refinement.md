---
"professional-design-system": minor
---

Added a `featured` prop to `Card` (renders the new accent-rail, elevated `.card-featured` variant) and refined the shared `.card`, `.data-table`/`.table`, `.btn`, and `.form-select` classes with real hover/focus depth and hierarchy — no breaking changes to existing markup or props.
